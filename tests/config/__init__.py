"""Tests for the configuration entry point: src/settings.py and src/config_errors.py.

A regular package (not a PEP 420 namespace portion) for the same reason as
tests/__init__.py — see the docstring there.
"""
